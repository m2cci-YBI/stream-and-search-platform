server:
  port: 8888

logging:
  level:
    ROOT: INFO
    org.spring.framework.cloud.config: INFO
spring:
  application:
    name: configserver
#  cloud:
#    config:
#      server:
#        git:
#          uri: https://github.com/m2cci-YBI/spring-cloud-config-repo
#          default-label: main
#          username: m2cci-YBI
#          password: *****************
#          clone-on-start: true
#    fail-fast: true

  security:
    user:
      name: spring_cloud_user
      password: '1234'

log:
  app-name: config-server

---
# Native profile: load only when SPRING_PROFILES_ACTIVE=native
spring:
  config:
    activate:
      on-profile: native
  cloud:
    config:
      server:
        native:
          search-locations: file:/workspace/local-config-repo